import 'package:flutter/material.dart';

class MedicHome extends StatelessWidget {
  const MedicHome({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('Medical Personnel')),
        body: const Center(child: Text('TODO: medic view')),
      );
}
